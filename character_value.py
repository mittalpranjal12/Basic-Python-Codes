#The code above to get characters from their corresponding ASCII values using the chr() function as shown below.
#chr() is a built-in function

val = int(input("Enter the number: "))

print("The character of given number is", chr(val))